PORT=5000
MONGO_URI=mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.t3v66j7.mongodb.net/TirTir-Project?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
# Token expiry: '7d' (default), '30d', '1y', '2y'
# Set to '1y' for long-lived admin/dev tokens
JWT_EXPIRE=7d

# Email Configuration (For Password Reset)
# For Gmail: Use App Password if 2FA is enabled
# For production: Use SendGrid, Mailgun, AWS SES, etc.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM_NAME=TirTir Shop
EMAIL_FROM=noreply@tirtir.com

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:4200

# Gemini AI Configuration (for Virtual Camera AI features)
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# AI Microservice (FastAPI) — Internal API Key
# Used for backend ↔ AI service authentication
AI_SERVICE_API_KEY=your_internal_ai_key_here
