<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/tirtir_off_white">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCommunityRoutines"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingStart="12dp"
        android:paddingTop="12dp"
        android:paddingEnd="12dp"
        android:paddingBottom="96dp" />

    <TextView
        android:id="@+id/tvCommunityEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="32dp"
        android:text="No community routines yet. Shared routines from the TirTir community will appear here."
        android:textColor="@color/tirtir_text_secondary"
        android:textSize="14sp" />
</FrameLayout>
