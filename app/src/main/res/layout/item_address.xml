<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/tirtir_milk_white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Row 1: Name + Default chip + More button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <TextView
                android:id="@+id/tvReceiverName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textStyle="bold"
                android:textColor="@color/tirtir_text_primary"
                android:textSize="15sp"
                android:text="Recipient name" />

            <com.google.android.material.chip.Chip
                android:id="@+id/tvAddressLabel"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="@string/address_default_label"
                android:textColor="@color/tirtir_red_primary"
                app:chipBackgroundColor="@color/tirtir_aura_pink"
                app:chipStrokeColor="@color/tirtir_red_primary"
                app:chipStrokeWidth="1dp"
                android:textSize="11sp"
                android:layout_marginEnd="8dp" />

            <ImageButton
                android:id="@+id/btnEditAddress"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_edit"
                android:contentDescription="@string/address_edit" />
        </LinearLayout>

        <!-- Row 2: Phone -->
        <TextView
            android:id="@+id/tvPhone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/tirtir_text_secondary"
            android:textSize="13sp"
            android:layout_marginBottom="4dp"
            android:text="0901234567" />

        <!-- Row 3: Address detail -->
        <TextView
            android:id="@+id/tvAddressLine"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/tirtir_text_secondary"
            android:textSize="13sp"
            android:maxLines="2"
            android:text="Shipping address" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
